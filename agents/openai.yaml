interface:
  display_name: "Model and Effort Selector"
  short_description: "Inspect project files, then choose model and effort"
  default_prompt: "Use $model-effort-selector to inspect the relevant files in my project, recommend a model and reasoning effort based on that evidence, and state when to escalate without changing settings."
